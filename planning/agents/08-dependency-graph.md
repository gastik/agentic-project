# Agent 08 — Task Dependency Graph

Write only `planning/output/TASK_DEPENDENCY_GRAPH.md`. Determine hard dependencies, independent tasks, execution waves, and tasks that unlock later work. The graph must be a DAG. Report FAIL on any cycle.
